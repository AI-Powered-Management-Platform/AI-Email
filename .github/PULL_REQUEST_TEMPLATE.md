## What this PR does

<!-- One sentence. One concern only (A1). -->

## Checklist

- [ ] `task check` passes locally — G1–G4, G6 (race G5 runs in CI)
- [ ] Changed behaviour has a test that fails without the change
- [ ] Under ~400 changed lines, or the split was discussed first (A1)
- [ ] Docs updated in this PR if behaviour changed
- [ ] Every commit is signed off — DCO, `git commit -s`

## Threat surface

<!-- Threat IDs this change touches (docs/threat-model.md, T1–T19), or "none". -->

## Hot paths

<!-- If internal/template, internal/store, or internal/dkim changed:
     benchmark before/after (G9), or "not touched". -->
